subscription latestTasks($userId: String) {
  latestTasks(userId: $userId) {
    _id,
    title,
    description,
    creationDate,
    state,
    orderIndex,
    modifidedDate,
    createdBy,
    modifidedBy,
    responsibleUser,
    plannedExecutionTime,
    actualExecutionTime
  }
}